{# @var plugin \vardumper\promptdb\PromptDb #}
{# @var settings \vardumper\promptdb\models\Settings #}
{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    first: true,
    label: 'Secret API Key',
    name: 'apiKey',
    id: 'apiKey',
    placeholder: 'sk-....',
    autofocus: true,
    autocomplete: false,
    value: settings.apiKey,
    min: 49,
    max: 54,
    length: 54,
    maxlength: 54,
}) }}

	<p>
	Find your Secret API Key in your
		<a
		href="https://beta.openai.com/account/api-keys">
		User settings
	</a>
	.
</p>


{{ forms.textField({
    first: true,
    label: 'User Email (optional)',
    name: 'user',
    id: 'user',
    placeholder: 'email@example.com',
    autofocus: true,
    autocomplete: false,
    value: settings.user
}) }}

<p>
	If provided, the email is sent along with every request to OpenAI. This is useful for security, debugging and support.
	<a
		href="https://platform.openai.com/docs/guides/safety-best-practices/end-user-ids">
		Read more
	</a>
</p>
